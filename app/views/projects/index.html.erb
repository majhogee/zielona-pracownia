<h1 >Zrealizowane projekty: </h1>

<table>
  <tr>
    <th>Projekt</th>
    <th>Opis</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @projects.each do |project| %>
  <tr>
    <td><%= project.name %></td>
    <td><%= project.email %></td>
    <td><%= link_to 'Show', project %></td>
    <td><%= link_to 'Edit', edit_project_path(project) %></td>
    <td><%= link_to 'Destroy', project, method: :delete,
    data: { confirm: 'Na pewno?' } %></td>
  </tr>
  <% end %>
</table>

<br />

